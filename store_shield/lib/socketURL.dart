// socketURL.dart
class SocketConfig {
  // 정적 문자열 변수로 socket.url 정의
  static const String socketURL = "https://0f85c3678785.ngrok.app";
  // 필요한 경우 URL을 가져오는 메서드
  static String getSocketURL() {
    return socketURL;
  }
}
